# App Visualization Layer

This layer is for transforming firmware telemetry into display-ready data.

Examples:

- Robot pose/path overlays for RViz and warehouse map views.
- Occupancy grid and costmap display transforms.
- Lidar point cloud rendering helpers.
- Diagnostic overlays for nav state, PID state, and safety status.

Keep runtime control decisions in firmware for the real robot.
